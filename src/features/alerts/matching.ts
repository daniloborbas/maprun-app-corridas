export type AlertPreferences={nearby_events_enabled:boolean;city_events_enabled:boolean;saved_event_reminders_enabled:boolean;preferred_radius_km:number;favorite_distances:number[];preferred_categories:string[];city?:string;state?:string;latitude?:number|null;longitude?:number|null};
export type AlertEvent={id?:string;start_date:string;city:string;state:string;latitude?:number|null;longitude?:number|null;event_category?:string;distances?:number[]};
const distance=(a:number,b:number,c:number,d:number)=>{const r=6371,x=(c-a)*Math.PI/180,y=(d-b)*Math.PI/180;return 2*r*Math.asin(Math.sqrt(Math.sin(y/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(x/2)**2));};
export function matchEventsToPreferences(p:AlertPreferences,events:AlertEvent[]){return events.filter(e=>new Date(e.start_date)>new Date()&&(!p.city_events_enabled||!p.city|| (e.city.toLowerCase()===p.city.toLowerCase()&&(!p.state||e.state===p.state)))&&(!p.nearby_events_enabled||(!p.latitude||!p.longitude||!e.latitude||!e.longitude||distance(p.latitude,p.longitude,e.latitude,e.longitude)<=p.preferred_radius_km))&&(!p.favorite_distances.length||e.distances?.some(d=>p.favorite_distances.includes(d)))&&(!p.preferred_categories.length||!e.event_category||p.preferred_categories.includes(e.event_category)));}
