export type DiscoveryCandidateStatus = 'pending'|'imported'|'ignored'|'duplicate'|'error';
export type DiscoveryQualityStatus = 'ready'|'incomplete'|'conflict';
export interface DiscoverySource { id: string; name: string; base_url: string; source_type: 'html_calendar'|'organizer_page'; active: boolean; region: string; trust_level?: 'A'|'B'|'C'; auto_ready_allowed?: boolean; last_checked_at?: string|null; }
export interface DiscoveredEventCandidate { id?: string; source_id: string; source_url: string; external_id?: string|null; name: string; event_date?: string|null; city?: string|null; state?: string|null; organizer_name?: string|null; registration_url?: string|null; cover_image_url?: string|null; raw_title?: string|null; status: DiscoveryCandidateStatus; quality_status?: DiscoveryQualityStatus; enriched_at?: string|null; duplicate_event_id?: string|null; discovered_at?: string; }
export interface DiscoveryRunSummary { discovered: number; newCandidates: number; known: number; duplicates: number; errors: number; sources: number; ignored: number; pastIgnored: number; enriched: number; ready: number; incomplete: number; conflicts: number; enrichmentErrors: number; }
